package NonPrimitiveConcepts;

import java.util.ArrayList;

public class Collection {

	/* collections is an interface , inbuilt in java
	 * collection is divided in interfaces as  List , set, map , which are interfaces as well
	 * List has two main classes , Array list and linked list .
	 * set again has two classes hased set and linked set 
	 * Map has two classes hash map and linked map 
	 * 
	 */
	ArrayList <String> list = new ArrayList <String>();
		
}
